import Granite
import GraniteUI
import SwiftUI
import LemmyKit

extension Globe: View {
    public var view: some View {
        VStack(spacing: 0) {
            mainView
            
        }
        .background(Color.background)
    }
}
